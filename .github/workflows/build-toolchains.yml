name: Build Toolchains

on:
  workflow_dispatch:
    inputs:
      packages:
        description: "Space-separated list of packages to build (e.g. 'gcc lua sqlite'), or 'all'"
        required: false
        default: "all"
  schedule:
    # Rebuild toolchains weekly on Sunday at 02:00 UTC.
    - cron: "0 2 * * 0"

permissions:
  contents: write

jobs:
  # ── Determine next sequential release tag ─────────────────────────────────
  next-tag:
    name: Determine next release tag
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compute next vN tag
        id: tag
        run: |
          MAX=0
          while IFS= read -r t; do
            N="${t#v}"
            if [[ "$N" =~ ^[0-9]+$ ]] && (( N > MAX )); then MAX=$N; fi
          done < <(git tag --list 'v[0-9]*')
          NEXT=$((MAX + 1))
          echo "tag=v${NEXT}" >> "$GITHUB_OUTPUT"
          echo "Next release tag: v${NEXT}"

  # ── Build matrix ───────────────────────────────────────────────────────────
  build:
    name: ${{ matrix.package }} (${{ matrix.os }}/${{ matrix.arch }})
    needs: next-tag
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:

          # ── SQLite (Linux amd64) ───────────────────────────────────────────
          - package: sqlite
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "3.46.1"
            source_url: "https://www.sqlite.org/2024/sqlite-autoconf-3460100.tar.gz"
            source_sha256: "67d3fe6d268e6eaddcae3727fce58fcc8e9c53869bdd07a0c61e38ddf2965071"
            build_type: autoconf

          # ── SQLite (Linux arm64) ───────────────────────────────────────────
          - package: sqlite
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "3.46.1"
            source_url: "https://www.sqlite.org/2024/sqlite-autoconf-3460100.tar.gz"
            source_sha256: "67d3fe6d268e6eaddcae3727fce58fcc8e9c53869bdd07a0c61e38ddf2965071"
            build_type: autoconf

          # ── SQLite (macOS arm64) ───────────────────────────────────────────
          - package: sqlite
            os: darwin
            arch: arm64
            runner: macos-14
            version: "3.46.1"
            source_url: "https://www.sqlite.org/2024/sqlite-autoconf-3460100.tar.gz"
            source_sha256: "67d3fe6d268e6eaddcae3727fce58fcc8e9c53869bdd07a0c61e38ddf2965071"
            build_type: autoconf

          # ── SQLite (Windows amd64) — cmake amalgamation ──────────────────
          - package: sqlite
            os: windows
            arch: amd64
            runner: windows-2022
            version: "3.46.1"
            # SQLite's download page lists SHA3-256, not SHA-256, so we leave
            # source_sha256 empty and rely on HTTPS for transport integrity.
            source_url: "https://www.sqlite.org/2024/sqlite-amalgamation-3460100.zip"
            source_sha256: ""
            build_type: cmake_amalgamation

          # ── zlib (Linux amd64) ────────────────────────────────────────────
          - package: zlib
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "1.3.1"
            source_url: "https://github.com/madler/zlib/releases/download/v1.3.1/zlib-1.3.1.tar.gz"
            source_sha256: "9a93b2b7dfdac77ceba5a558a580e74667dd6fede4585b91eefb60f03b72df23"
            build_type: cmake

          # ── zlib (macOS arm64) ────────────────────────────────────────────
          - package: zlib
            os: darwin
            arch: arm64
            runner: macos-14
            version: "1.3.1"
            source_url: "https://github.com/madler/zlib/releases/download/v1.3.1/zlib-1.3.1.tar.gz"
            source_sha256: "9a93b2b7dfdac77ceba5a558a580e74667dd6fede4585b91eefb60f03b72df23"
            build_type: cmake

          # ── zlib (Windows amd64) ─────────────────────────────────────────
          - package: zlib
            os: windows
            arch: amd64
            runner: windows-2022
            version: "1.3.1"
            source_url: "https://github.com/madler/zlib/releases/download/v1.3.1/zlib-1.3.1.tar.gz"
            source_sha256: "9a93b2b7dfdac77ceba5a558a580e74667dd6fede4585b91eefb60f03b72df23"
            build_type: cmake

          # ── Lua (Linux amd64) ─────────────────────────────────────────────
          - package: lua
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "5.4.7"
            source_url: "https://www.lua.org/ftp/lua-5.4.7.tar.gz"
            source_sha256: "9fbf5e28ef86c69858f6d3d34eccc32e911c1a28b4120ff3e84aaa70cfbf1e30"
            build_type: lua_make

          # ── Lua (macOS arm64) ─────────────────────────────────────────────
          - package: lua
            os: darwin
            arch: arm64
            runner: macos-14
            version: "5.4.7"
            source_url: "https://www.lua.org/ftp/lua-5.4.7.tar.gz"
            source_sha256: "9fbf5e28ef86c69858f6d3d34eccc32e911c1a28b4120ff3e84aaa70cfbf1e30"
            build_type: lua_make

          # ── Zig 0.13.0 prebuilt (Linux amd64) ────────────────────────────
          - package: zig
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "0.13.0"
            source_url: "https://ziglang.org/download/0.13.0/zig-linux-x86_64-0.13.0.tar.xz"
            # Fetched from https://ziglang.org/download/0.13.0/zig-0.13.0-release-notes.html
            source_sha256: "d45312e61ebcc48032b77bc4cf7fd6915c11fa16e4aad116b66c9468211230ea"
            build_type: zig_prebuilt

          # ── Zig 0.13.0 prebuilt (Linux arm64) ────────────────────────────
          - package: zig
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "0.13.0"
            source_url: "https://ziglang.org/download/0.13.0/zig-linux-aarch64-0.13.0.tar.xz"
            # From ziglang.org/download/index.json
            source_sha256: "041ac42323837eb5624068acd8b00cd5777dac4cf91179e8dad7a7e90dd0c556"
            build_type: zig_prebuilt

          # ── Zig 0.13.0 prebuilt (macOS arm64) ────────────────────────────
          - package: zig
            os: darwin
            arch: arm64
            runner: macos-14
            version: "0.13.0"
            # Note: 0.13.0 uses zig-macos-aarch64 naming (not zig-aarch64-macos)
            source_url: "https://ziglang.org/download/0.13.0/zig-macos-aarch64-0.13.0.tar.xz"
            # From ziglang.org/download/index.json
            source_sha256: "46fae219656545dfaf4dce12fb4e8685cec5b51d721beee9389ab4194d43394c"
            build_type: zig_prebuilt

          # ── Zig 0.13.0 prebuilt (Windows amd64) ──────────────────────────
          - package: zig
            os: windows
            arch: amd64
            runner: windows-2022
            version: "0.13.0"
            source_url: "https://ziglang.org/download/0.13.0/zig-windows-x86_64-0.13.0.zip"
            source_sha256: "d859994725ef9402381e557c60bb57497215682e355204d754ee3df75ee3c158"
            build_type: zig_prebuilt

          # ══════════════════════════════════════════════════════════════════
          # New packages below this line
          # ══════════════════════════════════════════════════════════════════

          # ── zstd (cmake, all 4 platforms) ─────────────────────────────────
          - package: zstd
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "1.5.6"
            source_url: "https://github.com/facebook/zstd/archive/refs/tags/v1.5.6.tar.gz"
            source_sha256: "30f35f71c1203369dc979ecde0400ffea93c27391bfd2ac5a9715d2173d92ff7"
            build_type: cmake
            cmake_source_subdir: "build/cmake"

          - package: zstd
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "1.5.6"
            source_url: "https://github.com/facebook/zstd/archive/refs/tags/v1.5.6.tar.gz"
            source_sha256: "30f35f71c1203369dc979ecde0400ffea93c27391bfd2ac5a9715d2173d92ff7"
            build_type: cmake
            cmake_source_subdir: "build/cmake"

          - package: zstd
            os: darwin
            arch: arm64
            runner: macos-14
            version: "1.5.6"
            source_url: "https://github.com/facebook/zstd/archive/refs/tags/v1.5.6.tar.gz"
            source_sha256: "30f35f71c1203369dc979ecde0400ffea93c27391bfd2ac5a9715d2173d92ff7"
            build_type: cmake
            cmake_source_subdir: "build/cmake"

          - package: zstd
            os: windows
            arch: amd64
            runner: windows-2022
            version: "1.5.6"
            source_url: "https://github.com/facebook/zstd/archive/refs/tags/v1.5.6.tar.gz"
            source_sha256: "30f35f71c1203369dc979ecde0400ffea93c27391bfd2ac5a9715d2173d92ff7"
            build_type: cmake
            cmake_source_subdir: "build/cmake"

          # ── libpng (cmake, all 4 platforms) ───────────────────────────────
          - package: libpng
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "1.6.58"
            source_url: "https://downloads.sourceforge.net/project/libpng/libpng16/1.6.58/libpng-1.6.58.tar.xz"
            source_sha256: "28eb403f51f0f7405249132cecfe82ea5c0ef97f1b32c5a65828814ae0d34775"
            build_type: cmake

          - package: libpng
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "1.6.58"
            source_url: "https://downloads.sourceforge.net/project/libpng/libpng16/1.6.58/libpng-1.6.58.tar.xz"
            source_sha256: "28eb403f51f0f7405249132cecfe82ea5c0ef97f1b32c5a65828814ae0d34775"
            build_type: cmake

          - package: libpng
            os: darwin
            arch: arm64
            runner: macos-14
            version: "1.6.58"
            source_url: "https://downloads.sourceforge.net/project/libpng/libpng16/1.6.58/libpng-1.6.58.tar.xz"
            source_sha256: "28eb403f51f0f7405249132cecfe82ea5c0ef97f1b32c5a65828814ae0d34775"
            build_type: cmake

          - package: libpng
            os: windows
            arch: amd64
            runner: windows-2022
            version: "1.6.58"
            source_url: "https://downloads.sourceforge.net/project/libpng/libpng16/1.6.58/libpng-1.6.58.tar.xz"
            source_sha256: "28eb403f51f0f7405249132cecfe82ea5c0ef97f1b32c5a65828814ae0d34775"
            build_type: cmake

          # ── libjpeg-turbo (cmake, all 4 platforms) ────────────────────────
          - package: libjpeg-turbo
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "3.1.3"
            source_url: "https://github.com/libjpeg-turbo/libjpeg-turbo/releases/download/3.1.3/libjpeg-turbo-3.1.3.tar.gz"
            source_sha256: "075920b826834ac4ddf97661cc73491047855859affd671d52079c6867c1c6c0"
            build_type: cmake

          - package: libjpeg-turbo
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "3.1.3"
            source_url: "https://github.com/libjpeg-turbo/libjpeg-turbo/releases/download/3.1.3/libjpeg-turbo-3.1.3.tar.gz"
            source_sha256: "075920b826834ac4ddf97661cc73491047855859affd671d52079c6867c1c6c0"
            build_type: cmake

          - package: libjpeg-turbo
            os: darwin
            arch: arm64
            runner: macos-14
            version: "3.1.3"
            source_url: "https://github.com/libjpeg-turbo/libjpeg-turbo/releases/download/3.1.3/libjpeg-turbo-3.1.3.tar.gz"
            source_sha256: "075920b826834ac4ddf97661cc73491047855859affd671d52079c6867c1c6c0"
            build_type: cmake

          - package: libjpeg-turbo
            os: windows
            arch: amd64
            runner: windows-2022
            version: "3.1.3"
            source_url: "https://github.com/libjpeg-turbo/libjpeg-turbo/releases/download/3.1.3/libjpeg-turbo-3.1.3.tar.gz"
            source_sha256: "075920b826834ac4ddf97661cc73491047855859affd671d52079c6867c1c6c0"
            build_type: cmake

          # ── pcre2 (cmake, all 4 platforms) ────────────────────────────────
          - package: pcre2
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "10.47"
            source_url: "https://github.com/PCRE2Project/pcre2/releases/download/pcre2-10.47/pcre2-10.47.tar.bz2"
            source_sha256: "47fe8c99461250d42f89e6e8fdaeba9da057855d06eb7fc08d9ca03fd08d7bc7"
            build_type: cmake

          - package: pcre2
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "10.47"
            source_url: "https://github.com/PCRE2Project/pcre2/releases/download/pcre2-10.47/pcre2-10.47.tar.bz2"
            source_sha256: "47fe8c99461250d42f89e6e8fdaeba9da057855d06eb7fc08d9ca03fd08d7bc7"
            build_type: cmake

          - package: pcre2
            os: darwin
            arch: arm64
            runner: macos-14
            version: "10.47"
            source_url: "https://github.com/PCRE2Project/pcre2/releases/download/pcre2-10.47/pcre2-10.47.tar.bz2"
            source_sha256: "47fe8c99461250d42f89e6e8fdaeba9da057855d06eb7fc08d9ca03fd08d7bc7"
            build_type: cmake

          - package: pcre2
            os: windows
            arch: amd64
            runner: windows-2022
            version: "10.47"
            source_url: "https://github.com/PCRE2Project/pcre2/releases/download/pcre2-10.47/pcre2-10.47.tar.bz2"
            source_sha256: "47fe8c99461250d42f89e6e8fdaeba9da057855d06eb7fc08d9ca03fd08d7bc7"
            build_type: cmake

          # ── expat (cmake, all 4 platforms) ────────────────────────────────
          - package: expat
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "2.8.2"
            source_url: "https://github.com/libexpat/libexpat/releases/download/R_2_8_2/expat-2.8.2.tar.xz"
            source_sha256: "3ad89b8588e6644bd4e49981480d48b21289eebbcd4f0a1a4afb1c29f99b6ab4"
            build_type: cmake

          - package: expat
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "2.8.2"
            source_url: "https://github.com/libexpat/libexpat/releases/download/R_2_8_2/expat-2.8.2.tar.xz"
            source_sha256: "3ad89b8588e6644bd4e49981480d48b21289eebbcd4f0a1a4afb1c29f99b6ab4"
            build_type: cmake

          - package: expat
            os: darwin
            arch: arm64
            runner: macos-14
            version: "2.8.2"
            source_url: "https://github.com/libexpat/libexpat/releases/download/R_2_8_2/expat-2.8.2.tar.xz"
            source_sha256: "3ad89b8588e6644bd4e49981480d48b21289eebbcd4f0a1a4afb1c29f99b6ab4"
            build_type: cmake

          - package: expat
            os: windows
            arch: amd64
            runner: windows-2022
            version: "2.8.2"
            source_url: "https://github.com/libexpat/libexpat/releases/download/R_2_8_2/expat-2.8.2.tar.xz"
            source_sha256: "3ad89b8588e6644bd4e49981480d48b21289eebbcd4f0a1a4afb1c29f99b6ab4"
            build_type: cmake

          # ── c-ares (cmake, all 4 platforms) ───────────────────────────────
          # NOTE: source_sha256 below could not be independently confirmed
          # against a citable checksum source during authoring — verify it
          # against the release's .asc signature or SLSA provenance before
          # relying on this in production.
          - package: c-ares
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "1.34.6"
            source_url: "https://github.com/c-ares/c-ares/releases/download/v1.34.6/c-ares-1.34.6.tar.gz"
            source_sha256: "PLEASE_VERIFY_SHA256"
            build_type: cmake

          - package: c-ares
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "1.34.6"
            source_url: "https://github.com/c-ares/c-ares/releases/download/v1.34.6/c-ares-1.34.6.tar.gz"
            source_sha256: "PLEASE_VERIFY_SHA256"
            build_type: cmake

          - package: c-ares
            os: darwin
            arch: arm64
            runner: macos-14
            version: "1.34.6"
            source_url: "https://github.com/c-ares/c-ares/releases/download/v1.34.6/c-ares-1.34.6.tar.gz"
            source_sha256: "PLEASE_VERIFY_SHA256"
            build_type: cmake

          - package: c-ares
            os: windows
            arch: amd64
            runner: windows-2022
            version: "1.34.6"
            source_url: "https://github.com/c-ares/c-ares/releases/download/v1.34.6/c-ares-1.34.6.tar.gz"
            source_sha256: "PLEASE_VERIFY_SHA256"
            build_type: cmake

          # ── libuv (cmake, all 4 platforms) ────────────────────────────────
          - package: libuv
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "1.51.0"
            source_url: "https://github.com/libuv/libuv/archive/refs/tags/v1.51.0.tar.gz"
            source_sha256: "27e55cf7083913bfb6826ca78cde9de7647cded648d35f24163f2d31bb9f51cd"
            build_type: cmake

          - package: libuv
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "1.51.0"
            source_url: "https://github.com/libuv/libuv/archive/refs/tags/v1.51.0.tar.gz"
            source_sha256: "27e55cf7083913bfb6826ca78cde9de7647cded648d35f24163f2d31bb9f51cd"
            build_type: cmake

          - package: libuv
            os: darwin
            arch: arm64
            runner: macos-14
            version: "1.51.0"
            source_url: "https://github.com/libuv/libuv/archive/refs/tags/v1.51.0.tar.gz"
            source_sha256: "27e55cf7083913bfb6826ca78cde9de7647cded648d35f24163f2d31bb9f51cd"
            build_type: cmake

          - package: libuv
            os: windows
            arch: amd64
            runner: windows-2022
            version: "1.51.0"
            source_url: "https://github.com/libuv/libuv/archive/refs/tags/v1.51.0.tar.gz"
            source_sha256: "27e55cf7083913bfb6826ca78cde9de7647cded648d35f24163f2d31bb9f51cd"
            build_type: cmake

          # ── ncurses (autoconf, Linux + macOS only — no official Windows
          #    autotools build without MSYS) ────────────────────────────────
          - package: ncurses
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "6.5"
            source_url: "https://ftp.gnu.org/gnu/ncurses/ncurses-6.5.tar.gz"
            source_sha256: "136d91bc269a9a5785e5f9e980bc76ab57428f604ce3e5a5a90cebc767971cc6"
            build_type: autoconf

          - package: ncurses
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "6.5"
            source_url: "https://ftp.gnu.org/gnu/ncurses/ncurses-6.5.tar.gz"
            source_sha256: "136d91bc269a9a5785e5f9e980bc76ab57428f604ce3e5a5a90cebc767971cc6"
            build_type: autoconf

          - package: ncurses
            os: darwin
            arch: arm64
            runner: macos-14
            version: "6.5"
            source_url: "https://ftp.gnu.org/gnu/ncurses/ncurses-6.5.tar.gz"
            source_sha256: "136d91bc269a9a5785e5f9e980bc76ab57428f604ce3e5a5a90cebc767971cc6"
            build_type: autoconf

          # ── xz / liblzma (autoconf, Linux + macOS only) ───────────────────
          # NOTE: pinned to 5.4.6 deliberately — 5.6.0/5.6.1 contained the
          # CVE-2024-3094 supply-chain backdoor. Do not bump to 5.6.0/5.6.1.
          - package: xz
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "5.4.6"
            source_url: "https://github.com/tukaani-project/xz/releases/download/v5.4.6/xz-5.4.6.tar.gz"
            source_sha256: "aeba3e03bf8140ddedf62a0a367158340520f6b384f75ca6045ccc6c0d43fd5c"
            build_type: autoconf

          - package: xz
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "5.4.6"
            source_url: "https://github.com/tukaani-project/xz/releases/download/v5.4.6/xz-5.4.6.tar.gz"
            source_sha256: "aeba3e03bf8140ddedf62a0a367158340520f6b384f75ca6045ccc6c0d43fd5c"
            build_type: autoconf

          - package: xz
            os: darwin
            arch: arm64
            runner: macos-14
            version: "5.4.6"
            source_url: "https://github.com/tukaani-project/xz/releases/download/v5.4.6/xz-5.4.6.tar.gz"
            source_sha256: "aeba3e03bf8140ddedf62a0a367158340520f6b384f75ca6045ccc6c0d43fd5c"
            build_type: autoconf

          # ── libffi (autoconf, Linux + macOS only) ─────────────────────────
          - package: libffi
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "3.4.6"
            source_url: "https://github.com/libffi/libffi/releases/download/v3.4.6/libffi-3.4.6.tar.gz"
            source_sha256: "b0dea9df23c863a7a50e825440f3ebffabd65df1497108e5d437747843895a4e"
            build_type: autoconf

          - package: libffi
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "3.4.6"
            source_url: "https://github.com/libffi/libffi/releases/download/v3.4.6/libffi-3.4.6.tar.gz"
            source_sha256: "b0dea9df23c863a7a50e825440f3ebffabd65df1497108e5d437747843895a4e"
            build_type: autoconf

          - package: libffi
            os: darwin
            arch: arm64
            runner: macos-14
            version: "3.4.6"
            source_url: "https://github.com/libffi/libffi/releases/download/v3.4.6/libffi-3.4.6.tar.gz"
            source_sha256: "b0dea9df23c863a7a50e825440f3ebffabd65df1497108e5d437747843895a4e"
            build_type: autoconf

          # ── bzip2 (plain Makefile, Linux + macOS only) ────────────────────
          - package: bzip2
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "1.0.8"
            source_url: "https://sourceware.org/pub/bzip2/bzip2-1.0.8.tar.gz"
            source_sha256: "ab5a03176ee106d3f0fa90e381da478ddae405918153cca248e682cd0c4a2269"
            build_type: bzip2_make

          - package: bzip2
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "1.0.8"
            source_url: "https://sourceware.org/pub/bzip2/bzip2-1.0.8.tar.gz"
            source_sha256: "ab5a03176ee106d3f0fa90e381da478ddae405918153cca248e682cd0c4a2269"
            build_type: bzip2_make

          - package: bzip2
            os: darwin
            arch: arm64
            runner: macos-14
            version: "1.0.8"
            source_url: "https://sourceware.org/pub/bzip2/bzip2-1.0.8.tar.gz"
            source_sha256: "ab5a03176ee106d3f0fa90e381da478ddae405918153cca248e682cd0c4a2269"
            build_type: bzip2_make

          # ── LuaJIT (plain Makefile, Linux + macOS only — Windows uses a
          #    separate msvcbuild.bat toolchain not covered here) ──────────
          - package: luajit
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "2.1.0-beta3"
            source_url: "http://luajit.org/download/LuaJIT-2.1.0-beta3.tar.gz"
            source_sha256: "1ad2e34b111c802f9d0cdf019e986909123237a28c746b21295b63c9e785d9c3"
            build_type: luajit_make

          - package: luajit
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "2.1.0-beta3"
            source_url: "http://luajit.org/download/LuaJIT-2.1.0-beta3.tar.gz"
            source_sha256: "1ad2e34b111c802f9d0cdf019e986909123237a28c746b21295b63c9e785d9c3"
            build_type: luajit_make

          - package: luajit
            os: darwin
            arch: arm64
            runner: macos-14
            version: "2.1.0-beta3"
            source_url: "http://luajit.org/download/LuaJIT-2.1.0-beta3.tar.gz"
            source_sha256: "1ad2e34b111c802f9d0cdf019e986909123237a28c746b21295b63c9e785d9c3"
            build_type: luajit_make

          # ── mruby (rake-based build, Linux amd64 + macOS arm64 only) ──────
          - package: mruby
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "3.3.0"
            source_url: "https://github.com/mruby/mruby/archive/refs/tags/3.3.0.tar.gz"
            source_sha256: "53088367e3d7657eb722ddfacb938f74aed1f8538b3717fe0b6eb8f58402af65"
            build_type: mruby_rake

          - package: mruby
            os: darwin
            arch: arm64
            runner: macos-14
            version: "3.3.0"
            source_url: "https://github.com/mruby/mruby/archive/refs/tags/3.3.0.tar.gz"
            source_sha256: "53088367e3d7657eb722ddfacb938f74aed1f8538b3717fe0b6eb8f58402af65"
            build_type: mruby_rake

          # ── musl libc (Linux only — it's a libc replacement, not
          #    applicable to macOS or Windows) ────────────────────────────
          # NOTE: source_sha256 below could not be independently confirmed
          # against a citable checksum source during authoring — verify it
          # against musl-1.2.5.tar.gz.asc before relying on this build.
          - package: musl
            os: linux
            arch: amd64
            runner: ubuntu-22.04
            version: "1.2.5"
            source_url: "https://musl.libc.org/releases/musl-1.2.5.tar.gz"
            source_sha256: "PLEASE_VERIFY_SHA256"
            build_type: musl_configure

          - package: musl
            os: linux
            arch: arm64
            runner: ubuntu-22.04-arm
            version: "1.2.5"
            source_url: "https://musl.libc.org/releases/musl-1.2.5.tar.gz"
            source_sha256: "PLEASE_VERIFY_SHA256"
            build_type: musl_configure

    steps:
      - uses: actions/checkout@v4

      # ── Platform-specific tool installation ──────────────────────────────
      - name: Install tools (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake ninja-build pkg-config \
            zlib1g-dev xz-utils curl zstd bison ruby-dev

      - name: Install tools (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install cmake ninja zstd xz bison

      - name: Install tools (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install -y cmake --installargs 'ADD_CMAKE_TO_PATH=System'
          choco install -y ninja 7zip

      # ── Download source ───────────────────────────────────────────────────
      - name: Download source archive
        shell: bash
        run: |
          mkdir -p build/src
          ARCHIVE_NAME=$(basename "${{ matrix.source_url }}")
          curl -fsSL --retry 5 --retry-delay 3 \
            -o "build/src/${ARCHIVE_NAME}" "${{ matrix.source_url }}"
          echo "Downloaded ${ARCHIVE_NAME}"

      # ── Verify checksum (cross-platform) ─────────────────────────────────
      - name: Verify source checksum (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          ARCHIVE_NAME=$(basename "${{ matrix.source_url }}")
          echo "${{ matrix.source_sha256 }}  build/src/${ARCHIVE_NAME}" | sha256sum --check
          echo "✓ Checksum verified"

      - name: Verify source checksum (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          ARCHIVE_NAME=$(basename "${{ matrix.source_url }}")
          echo "${{ matrix.source_sha256 }}  build/src/${ARCHIVE_NAME}" | shasum -a 256 --check
          echo "✓ Checksum verified"

      - name: Verify source checksum (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $expected = "${{ matrix.source_sha256 }}"
          if ($expected -eq "") {
            Write-Host "No SHA-256 provided for this package (SQLite uses SHA3-256); skipping checksum step."
          } else {
            $archive = "build/src/$((Split-Path '${{ matrix.source_url }}' -Leaf))"
            $actual = (Get-FileHash $archive -Algorithm SHA256).Hash.ToLower()
            if ($actual -ne $expected) {
              Write-Error "Checksum mismatch: expected $expected, got $actual"
              exit 1
            }
            Write-Host "✓ Checksum verified"
          }

      # ── Extract source ────────────────────────────────────────────────────
      - name: Extract source (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          ARCHIVE_NAME=$(basename "${{ matrix.source_url }}")
          mkdir -p build/source
          case "${ARCHIVE_NAME}" in
            *.tar.xz|*.tar.gz|*.tar.bz2|*.tar.zst)
              tar -xf "build/src/${ARCHIVE_NAME}" -C build/source --strip-components=1
              ;;
            *.zip)
              TMP=$(mktemp -d)
              unzip -q "build/src/${ARCHIVE_NAME}" -d "${TMP}"
              # Move contents of the single top-level directory.
              INNER=$(find "${TMP}" -mindepth 1 -maxdepth 1 -type d | head -1)
              mv "${INNER}"/* build/source/
              ;;
          esac
          echo "Source extracted."

      - name: Extract source (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $archive = "build/src/$((Split-Path '${{ matrix.source_url }}' -Leaf))"
          New-Item -ItemType Directory -Force build\source | Out-Null
          $ext = $archive -replace '.*(\.[^.]+)$','$1'
          if ($archive -match '\.zip$') {
            $tmp = New-TemporaryFile | ForEach-Object { Remove-Item $_; New-Item -Type Directory $_ }
            Expand-Archive -Path $archive -DestinationPath $tmp
            $inner = Get-ChildItem $tmp | Select-Object -First 1
            Get-ChildItem $inner.FullName | ForEach-Object { Move-Item $_.FullName build\source\ }
          } elseif ($archive -match '\.tar\.(gz|xz|bz2)$') {
            tar -xf $archive -C build\source --strip-components=1
          }
          Write-Host "Source extracted."

      # ── Build: autoconf (SQLite Linux/macOS, ncurses, xz, libffi) ────────
      - name: Build (autoconf)
        if: matrix.build_type == 'autoconf'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          mkdir -p build/build_dir
          cd build/build_dir
          ../source/configure \
            --prefix="${INSTALL_DIR}" \
            --enable-shared \
            --enable-static \
            --enable-threadsafe \
            --disable-werror 2>&1 | tail -5
          make -j$(nproc 2>/dev/null || sysctl -n hw.ncpu)
          make install

      # ── Build: cmake (zlib, zstd, libpng, libjpeg-turbo, pcre2, expat,
      #    c-ares, libuv, all platforms) ────────────────────────────────────
      - name: Build (cmake) Unix
        if: matrix.build_type == 'cmake' && runner.os != 'Windows'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          SRC_DIR="build/source"
          if [ -n "${{ matrix.cmake_source_subdir }}" ]; then
            SRC_DIR="build/source/${{ matrix.cmake_source_subdir }}"
          fi
          cmake -S "${SRC_DIR}" -B build/build_dir \
            -DCMAKE_INSTALL_PREFIX="${INSTALL_DIR}" \
            -DBUILD_SHARED_LIBS=ON \
            -DCMAKE_BUILD_TYPE=Release \
            -G Ninja
          cmake --build build/build_dir --parallel
          cmake --install build/build_dir

      - name: Build (cmake) Windows
        if: matrix.build_type == 'cmake' && runner.os == 'Windows'
        shell: pwsh
        run: |
          $INSTALL_DIR = "$PWD\build\install"
          $SRC_DIR = "build\source"
          if ("${{ matrix.cmake_source_subdir }}" -ne "") {
            $SRC_DIR = "build\source\${{ matrix.cmake_source_subdir }}"
          }
          cmake -S $SRC_DIR -B build\build_dir `
            -DCMAKE_INSTALL_PREFIX="$INSTALL_DIR" `
            -DBUILD_SHARED_LIBS=ON `
            -DCMAKE_BUILD_TYPE=Release
          cmake --build build\build_dir --config Release --parallel
          cmake --install build\build_dir --config Release

      # ── Build: cmake amalgamation (SQLite Windows) ───────────────────────
      - name: Build SQLite (cmake amalgamation, Windows)
        if: matrix.build_type == 'cmake_amalgamation'
        shell: pwsh
        run: |
          # The amalgamation zip contains sqlite3.c, sqlite3.h, sqlite3ext.h, shell.c
          $SRC = "$PWD\build\source"
          $BUILD = "$PWD\build\build_dir"
          $INSTALL = "$PWD\build\install"
          New-Item -ItemType Directory -Force $BUILD, "$INSTALL\bin", "$INSTALL\lib", "$INSTALL\include" | Out-Null

          # Write a minimal CMakeLists.txt to build the amalgamation
          @"
          cmake_minimum_required(VERSION 3.16)
          project(sqlite3 C)
          add_library(sqlite3 SHARED sqlite3.c)
          target_compile_definitions(sqlite3 PRIVATE
            SQLITE_ENABLE_MATH_FUNCTIONS SQLITE_ENABLE_FTS5
            SQLITE_ENABLE_RTREE SQLITE_THREADSAFE=1 SQLITE_DQS=0)
          set_target_properties(sqlite3 PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
          add_executable(sqlite3_shell shell.c)
          target_link_libraries(sqlite3_shell sqlite3)
          set_target_properties(sqlite3_shell PROPERTIES OUTPUT_NAME sqlite3)
          install(TARGETS sqlite3 sqlite3_shell
            RUNTIME DESTINATION bin
            LIBRARY DESTINATION lib
            ARCHIVE DESTINATION lib)
          install(FILES sqlite3.h sqlite3ext.h DESTINATION include)
          "@ | Set-Content "$SRC\CMakeLists.txt"

          cmake -S $SRC -B $BUILD -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=$INSTALL
          cmake --build $BUILD --config Release --parallel
          cmake --install $BUILD --config Release

      # ── Build: Lua (Linux/macOS) ──────────────────────────────────────────
      - name: Build Lua (make)
        if: matrix.build_type == 'lua_make'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          mkdir -p "${INSTALL_DIR}"
          cd build/source

          # Determine Lua make platform target
          case "${{ matrix.os }}" in
            linux)  PLAT=linux ;;
            darwin) PLAT=macosx ;;
            *)      PLAT=posix ;;
          esac

          NPROC=$(nproc 2>/dev/null || sysctl -n hw.ncpu 2>/dev/null || echo 4)

          # Build first, then install — Lua's Makefile requires this order.
          make -j${NPROC} MYCFLAGS="-fPIC" ${PLAT}
          make install INSTALL_TOP="${INSTALL_DIR}"

      # ── Build: bzip2 (plain Makefile, Linux/macOS) ────────────────────────
      - name: Build bzip2 (make)
        if: matrix.build_type == 'bzip2_make'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          mkdir -p "${INSTALL_DIR}"
          cd build/source

          NPROC=$(nproc 2>/dev/null || sysctl -n hw.ncpu 2>/dev/null || echo 4)

          # bzip2's Makefile predates -fPIC defaults; build the shared lib
          # target explicitly in addition to the static lib + CLI.
          sed -i.bak 's/^CFLAGS=\(.*\)$/CFLAGS=\1 -fPIC/' Makefile Makefile-libbz2_so || true
          make -j${NPROC}
          make -f Makefile-libbz2_so -j${NPROC} || true
          make install PREFIX="${INSTALL_DIR}"
          cp -f libbz2.so* "${INSTALL_DIR}/lib/" 2>/dev/null || true

      # ── Build: LuaJIT (plain Makefile, Linux/macOS) ───────────────────────
      - name: Build LuaJIT (make)
        if: matrix.build_type == 'luajit_make'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          mkdir -p "${INSTALL_DIR}"
          cd build/source

          NPROC=$(nproc 2>/dev/null || sysctl -n hw.ncpu 2>/dev/null || echo 4)

          make -j${NPROC} PREFIX="${INSTALL_DIR}"
          make install PREFIX="${INSTALL_DIR}"
          echo "LuaJIT version: $("${INSTALL_DIR}/bin/luajit" -v)"

      # ── Build: mruby (rake, Linux amd64 + macOS arm64) ────────────────────
      - name: Build mruby (rake)
        if: matrix.build_type == 'mruby_rake'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          mkdir -p "${INSTALL_DIR}"
          cd build/source

          cp build_config/default.rb homebrew.rb
          sed -i.bak "s/conf.gembox 'default'/conf.gembox 'full-core'/" homebrew.rb || \
            sed -i '' "s/conf.gembox 'default'/conf.gembox 'full-core'/" homebrew.rb

          MRUBY_CONFIG="${PWD}/homebrew.rb" rake

          mkdir -p "${INSTALL_DIR}/lib" "${INSTALL_DIR}/bin" "${INSTALL_DIR}/include"
          cp -f build/host/lib/*.a "${INSTALL_DIR}/lib/"
          cp -rf build/host/bin/* "${INSTALL_DIR}/bin/"
          cp -rf include/* "${INSTALL_DIR}/include/"
          echo "mruby build complete."

      # ── Build: musl libc (configure + make, Linux only) ───────────────────
      - name: Build musl (configure)
        if: matrix.build_type == 'musl_configure'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          mkdir -p "${INSTALL_DIR}"
          cd build/source

          NPROC=$(nproc 2>/dev/null || echo 4)

          ./configure \
            --prefix="${INSTALL_DIR}" \
            --syslibdir="${INSTALL_DIR}/lib"
          make -j${NPROC}
          make install
          echo "musl built for ${{ matrix.arch }}."

      # ── Build: Zig (prebuilt repackage) ──────────────────────────────────
      - name: Repackage Zig (Unix)
        if: matrix.build_type == 'zig_prebuilt' && runner.os != 'Windows'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          mkdir -p "${INSTALL_DIR}/bin"
          cp build/source/zig "${INSTALL_DIR}/bin/zig"
          chmod +x "${INSTALL_DIR}/bin/zig"
          # Include stdlib
          cp -r build/source/lib "${INSTALL_DIR}/" 2>/dev/null || true
          cp -r build/source/doc "${INSTALL_DIR}/" 2>/dev/null || true
          echo "Zig version: $("${INSTALL_DIR}/bin/zig" version)"

      - name: Repackage Zig (Windows)
        if: matrix.build_type == 'zig_prebuilt' && runner.os == 'Windows'
        shell: pwsh
        run: |
          $INSTALL = "$PWD\build\install"
          New-Item -ItemType Directory -Force "$INSTALL\bin" | Out-Null
          Copy-Item build\source\zig.exe "$INSTALL\bin\zig.exe"
          if (Test-Path build\source\lib)  { Copy-Item -Recurse build\source\lib  "$INSTALL\lib" }
          if (Test-Path build\source\doc)  { Copy-Item -Recurse build\source\doc  "$INSTALL\doc" }
          $ver = & "$INSTALL\bin\zig.exe" version
          Write-Host "Zig version: $ver"

      # ── Package artifact (Unix: tar.zst) ─────────────────────────────────
      - name: Package artifact (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          INSTALL_DIR="${PWD}/build/install"
          ARTIFACT_DIR="${PWD}/artifacts"
          mkdir -p "${ARTIFACT_DIR}"

          # Verify something was actually installed
          if [ -z "$(ls -A ${INSTALL_DIR} 2>/dev/null)" ]; then
            echo "ERROR: install directory is empty!"
            exit 1
          fi

          ARTIFACT_NAME="${{ matrix.package }}-${{ matrix.os }}-${{ matrix.arch }}"
          ARTIFACT_FILE="${ARTIFACT_NAME}.tar.zst"

          tar -C "${INSTALL_DIR}" \
              -c . \
              | zstd -T0 -19 -o "${ARTIFACT_DIR}/${ARTIFACT_FILE}"

          cd "${ARTIFACT_DIR}"
          # macOS uses shasum; Linux uses sha256sum
          if command -v sha256sum &>/dev/null; then
            sha256sum "${ARTIFACT_FILE}" > "${ARTIFACT_FILE}.sha256"
          else
            shasum -a 256 "${ARTIFACT_FILE}" > "${ARTIFACT_FILE}.sha256"
          fi
          echo "Packaged: ${ARTIFACT_FILE} ($(du -sh ${ARTIFACT_FILE} | cut -f1))"

      # ── Package artifact (Windows: zip) ──────────────────────────────────
      - name: Package artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $INSTALL = "$PWD\build\install"
          $ARTIFACTS = "$PWD\artifacts"
          New-Item -ItemType Directory -Force $ARTIFACTS | Out-Null

          if (-not (Get-ChildItem $INSTALL -ErrorAction SilentlyContinue)) {
            Write-Error "Install directory is empty!"
            exit 1
          }

          $ARTIFACT_NAME = "${{ matrix.package }}-${{ matrix.os }}-${{ matrix.arch }}"
          $ARTIFACT_FILE = "${ARTIFACT_NAME}.zip"

          # Use 7zip for reliable Windows zip creation
          & "C:\Program Files\7-Zip\7z.exe" a -tzip "$ARTIFACTS\$ARTIFACT_FILE" "$INSTALL\*"

          $hash = (Get-FileHash "$ARTIFACTS\$ARTIFACT_FILE" -Algorithm SHA256).Hash.ToLower()
          "$hash  $ARTIFACT_FILE" | Set-Content "$ARTIFACTS\$ARTIFACT_FILE.sha256"
          Write-Host "Packaged: $ARTIFACT_FILE"

      # ── Upload ────────────────────────────────────────────────────────────
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.package }}-${{ matrix.os }}-${{ matrix.arch }}
          path: artifacts/
          retention-days: 7

  # ── Generate manifest and publish release ─────────────────────────────────
  release:
    name: Publish release ${{ needs.next-tag.outputs.tag }}
    needs: [next-tag, build]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          merge-multiple: true

      - name: Generate manifest.json
        env:
          RELEASE_TAG: ${{ needs.next-tag.outputs.tag }}
          REPO: kompa-tbm/kompa
        run: |
          python3 - << 'PYEOF'
          import json, os, glob
          from datetime import datetime, timezone

          tag = os.environ["RELEASE_TAG"]
          repo = os.environ["REPO"]
          base_url = f"https://github.com/{repo}/releases/download/{tag}"
          artifact_dir = "release-artifacts"

          packages = []
          for sha_file in sorted(glob.glob(f"{artifact_dir}/**/*.sha256", recursive=True)):
              with open(sha_file) as f:
                  line = f.read().strip()
              parts = line.split(None, 1)
              if len(parts) != 2:
                  continue
              sha256 = parts[0]
              archive_name = os.path.basename(parts[1].strip())

              base = archive_name
              for ext in [".tar.zst", ".tar.gz", ".tar.xz", ".zip"]:
                  if base.endswith(ext):
                      base = base[:-len(ext)]
                      break

              segs = base.split("-")
              if len(segs) < 3:
                  continue
              pkg_name = segs[0]
              pkg_os   = segs[1]
              pkg_arch = "-".join(segs[2:])

              archive_path = os.path.join(os.path.dirname(sha_file), archive_name)
              size = os.path.getsize(archive_path) if os.path.exists(archive_path) else 0

              packages.append({
                  "schema_version": 1,
                  "name": pkg_name,
                  "version": "latest",
                  "platform": f"{pkg_os}-{pkg_arch}",
                  "os": pkg_os,
                  "architecture": pkg_arch,
                  "archive": archive_name,
                  "sha256": sha256,
                  "size": size,
                  "download_url": f"{base_url}/{archive_name}",
                  "dependencies": [],
                  "build_time": datetime.now(timezone.utc).isoformat(),
                  "kompa_version": "0.1.0",
                  "release_tag": tag,
              })

          manifest = {
              "schema_version": 1,
              "release_tag": tag,
              "build_time": datetime.now(timezone.utc).isoformat(),
              "kompa_version": "0.1.0",
              "packages": packages,
          }
          out = "release-artifacts/manifest.json"
          with open(out, "w") as f:
              json.dump(manifest, f, indent=2)
          print(f"Generated manifest with {len(packages)} package entries → {out}")
          PYEOF

      - name: Create and push tag
        run: |
          TAG="${{ needs.next-tag.outputs.tag }}"
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "${TAG}" -m "Toolchain release ${TAG}"
          git push origin "${TAG}"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.next-tag.outputs.tag }}
          name: "Kompa Toolchains ${{ needs.next-tag.outputs.tag }}"
          files: release-artifacts/**
          generate_release_notes: false
          body: |
            ## Toolchain Release ${{ needs.next-tag.outputs.tag }}

            Pre-built packages from official upstream sources, verified by SHA-256.

            ### Install
            ```bash
            kompa install sqlite
            kompa install zlib
            kompa install lua
            kompa install zig
            ```

            The `manifest.json` attached to this release lists all packages with their checksums and download URLs.